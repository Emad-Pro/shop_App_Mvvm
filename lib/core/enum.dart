enum RequestState {
  loadingState,
  loadedState,
  erorrState,
}

enum RequestStateCart {
  normalState,
  loadingState,
  loadedState,
  erorrState,
}

enum ConnectonState {
  offlineState,
  onlineState,
}
